from django.apps import AppConfig


class AppraisalsConfig(AppConfig):
    name = 'appraisals'
    # Client-facing label (the code identifier stays "appraisals").
    verbose_name = "Goal Setting and Review"
